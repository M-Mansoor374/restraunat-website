import React from 'react';
import RestaurantApp from './RestaurantApp';
import './RestaurantApp.css';

const Cart = () => {
  // Return the full restaurant app instead of just the cart
  return <RestaurantApp />;
};

export default Cart;